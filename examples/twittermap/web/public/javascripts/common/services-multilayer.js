angular.module('cloudberry.common')
    .service('multilayerService', function(multilayerHeatmap){
        var multilayerService = {
            createHeatmapLayer: multilayer-heatmap.createLayer;
        }
        
        return multilayerService;
    });
